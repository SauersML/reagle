# Block-Hash HMM

This module implements a specialized HMM for genotype imputation using a block-hashing approach for efficiency.

## Overview
- Uses global haplotype IDs and pattern IDs.
- Implements `CompressedBlock` for storage.
- Implements `TransitionBridge` for calculating transition probabilities.
